[package]
name = "runtime"
version = "0.1.1"
edition = "2024"

[lib]
name = "runtime"
path = "src/lib.rs"

[dependencies]
hex = "0.4"
serde = { version = "1.0.228", features = ["derive"] }
sha2 = "0.10"
