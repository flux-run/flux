/// Generated gRPC types for the `flux.internal.v1` service.
///
/// Used by all three binaries:
/// - `flux`         — CLI client
/// - `flux-server`  — gRPC server implementation
/// - `flux-runtime` — runtime gRPC client
pub mod pb {
    tonic::include_proto!("flux.internal.v1");
}
