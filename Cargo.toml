[workspace]
resolver = "2"
members = [
    "cli", "flux-executor",
    "runtime",
    "server",
    "shared",
]

[patch.crates-io]
# Force stable getrandom to avoid the unstable v0.4/edition2024 bug
getrandom = { git = "https://github.com/rust-random/getrandom.git", tag = "v0.2.15" }
