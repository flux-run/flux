{
  "name": "fluxbase",
  "version": "0.1.0",
  "private": true,
  "description": "Fluxbase — Serverless function platform monorepo",
  "workspaces": [
    "dashboard",
    "packages/functions",
    "test_functions/hello",
    "test_functions/echo",
    "test_functions/math"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=dashboard",
    "build": "npm run build --workspace=dashboard",
    "preview": "npm run preview --workspace=dashboard"
  },
  "engines": {
    "node": ">=20"
  }
}
